name: AP-Client-Docker-waterfall

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag autoplug-client:waterfall_$(date +%s) --build-arg PROXY_SERVER=1 --build-arg SERVER_SOFTWARE=waterfall